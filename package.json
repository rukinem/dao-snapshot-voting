{
  "name": "dao-snapshot-voting",
  "version": "1.0.0",
  "description": "Off-chain snapshot voting integration with on-chain execution bridge",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0"
  },
  "author": "rukinem",
  "license": "MIT"
}